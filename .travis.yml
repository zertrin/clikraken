language: python
python: '3.6'
sudo: false
env:
  matrix:
    - TOXENV=flake8
    - TOXENV=py34
    - TOXENV=py35
    - TOXENV=py36

before_install:
  - uname -a
  - lsb_release -a
  - python --version
  - virtualenv --version
  - easy_install --version
  - pip --version
install:
  - pip install tox
  - tox --version
  - python setup.py develop
script:
  - tox -v
after_failure:
  - more .tox/log/* | cat
  - more .tox/*/log/* | cat
before_cache:
  - rm -rf $HOME/.cache/pip/log
cache:
  directories:
    - $HOME/.cache/pip
notifications:
  email:
    on_success: never
    on_failure: never
